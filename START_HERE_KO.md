# JustVerify — Codex에서 끝까지 연속 수행하기

이 문서는 최초 개발 지시와 재개 방법을 보존한 기록이다. 현재 설치 후보는 0.1.0-beta1이며, 설치는 [한국어 README](docs/ko/README.md), 실제 완료·미완료 상태는 [ACCEPTANCE](docs/ACCEPTANCE.md)를 따른다.

이번 개정은 중간 단계마다 사용자에게 다음 작업을 물어보지 않고 S0부터 S4까지 구현·검증을 이어가는 것을 목표로 한다. “완벽”은 결함이 절대 없다는 보증이 아니라, 명시한 요구사항과 실제 검증 기준을 충족한 상태로 판정한다.

## 사용 방법

1. 압축을 풀어 나온 `justverify` 폴더를 원하는 개발 위치에 둔다.
2. Codex 앱에서 이 폴더를 프로젝트로 연다.
3. 아래 시작 지시문을 입력한다. 두 문서의 내용을 다시 타이핑할 필요는 없다.

```text
AGENTS.md와 PROJECT_BRIEF_KO.md를 모두 읽고 JustVerify 프로젝트를 시작해줘.

최종 목표는 PC통신 ANSI 느낌의 차트 없는 통합 TUI, justverify.local 브라우저 접속,
Bitcoin Core·Tor·electrs 통합, 모바일 연결 QR, 버전별 mempool 설정,
Core 22부터 최신 안정판까지의 선택, 그리고 쉽게 설치할 수 있는 부팅 이미지야.

문서의 기본안대로 Raspberry Pi 5를 우선 대상으로 진행하고 x86_64 확장성을 유지해줘.
이번 작업 범위는 S0부터 S4까지 전체이며, 최종 목표는 설치·운영 검증을 마친 배포 후보야.
단계 완료 때마다 내 승인이나 “계속해”라는 메시지를 기다리지 말고 다음 단계로 진행해줘.
일반적인 설계·라이브러리·구현 선택은 안정성, 유지보수성, 쉬운 배포를 기준으로 직접 결정해줘.
진행 상황은 응답을 요구하지 않는 짧은 알림과 docs/STATUS.md에 기록해줘.

설계 → 구현 → 빌드 → 실제 통합시험 → 오류 수정 → 재시험 → 배포 이미지 검증을 수행해줘.
필요한 검사에 실패하면 원인을 분석해 수정하고 다시 확인해줘.
테스트 삭제, 기준 완화, 요구사항 누락, mock으로 실제 통합을 대체하는 방식으로 통과시키지 마.
요구사항과 실제 실행 증거를 연결한 완료표를 유지하고, 통과한 검증은 변경 영향이 있을 때만 반복해줘.

환경과 접근권한은 먼저 직접 점검해줘. 부족한 항목이 있어도 진행 가능한 작업은 모두 수행해줘.
내가 해결해야만 하는 장비·인증·권한 문제는 시도한 해결법과 최소 필요 조치를 모아서 알려줘.
이미 허용된 범위의 작업은 다시 허락을 구하지 마.
실제 디스크 기록·삭제, 유료 서비스, 공개 배포는 사전에 허용된 대상과 범위를 지켜줘.

이미지 파일, 소스, 자동 검증 명령, 검증 결과, 설치·복구 안내를 함께 완성해줘.
실제 Raspberry Pi 부팅과 휴대폰 연결을 검증하지 못하면 전체 완료라고 보고하지 마.
컨텍스트 정리나 실행 중단에 대비해 작업 상태·실패 원인·재개 지점을 계속 저장해줘.
자동 재개 기능이 실제로 구성되지 않았다면 중단 후에도 계속 작업한다고 말하지 마.

실제 실행한 검증과 아직 실행하지 못한 검증을 구분해 보고해줘.
기존 노드 데이터나 실제 디스크는 변경하지 말고 독립된 개발 환경에서 시작해줘.
```

## 실행 환경 때문에 중단된 작업을 재개할 때

다음 문구는 매 단계에 필요한 지시가 아니다. 사용량 한도, 세션 종료, 기기 연결 문제 등으로 실행 자체가 중단되었을 때를 위한 복구 수단이다.

```text
AGENTS.md, PROJECT_BRIEF_KO.md, docs/STATUS.md를 읽고 중단 지점부터 S4까지 연속 수행해줘.
이미 확인한 사실과 테스트 결과는 재사용하고, 남은 위험을 해결하는 검증에 집중해줘.
최종 설치 이미지와 실기 검증까지 남은 항목을 업데이트해줘.
```

## 변경 가능한 기본안

현재 기본안은 Raspberry Pi 5 + 유선 LAN + SSD, Rust/Ratatui, systemd 기반이다. 사용자가 확정한 것은 제품 요구사항이며, 하드웨어·구현 기술은 실행 가능한 출발점으로 제안한 것이다. x86 미니PC를 먼저 지원하고 싶다면 Codex에 우선순위만 변경하도록 지시하면 된다.

Mac은 개발 환경으로 사용할 수 있다. Linux 서비스와 부팅 이미지의 빌드·실행 검증에는 적절한 Linux 환경 및 대상 장치가 필요하다. Mac에서 TUI가 실행된 것만으로 Raspberry Pi 이미지가 검증된 것은 아니다.

## 중간 개입을 줄이기 위한 최초 준비

Codex가 개발 폴더, 필요한 다운로드, 격리된 Linux 빌드·시험 환경에 접근할 수 있도록 준비한다. 실기 검증까지 맡기려면 시험용 Raspberry Pi와 데이터 삭제가 허용된 저장장치를 명확히 지정하고, 재부팅 후에도 다시 접속할 수 있어야 한다. 휴대폰 QR 실검증에는 실제 앱·카메라에 접근할 수 있는 시험 환경 또는 마지막 한 번의 사용자 확인이 필요하다. 앱 자동화·에뮬레이터 테스트만으로 실제 카메라 스캔을 완료했다고 판정하지 않는다.

프롬프트만으로 앱의 권한·인증·사용량 한도가 바뀌지는 않는다. 필요한 권한은 프로젝트와 시험 환경에 맞춰 준비한다. 개인 Mac 전체의 보호 기능을 해제하는 것을 기본 해결책으로 삼지 않는다. [공식 권한 문서](https://learn.chatgpt.com/docs/permissions)

앱의 한 실행을 넘어 자동 재개가 필요하다면 별도의 작업 실행기를 구성할 수 있다. Codex의 `codex exec`는 스크립트 실행과 세션 재개를 지원한다. 실제 적용 시 설치된 버전의 명령과 인증·권한을 확인하고, 특정 프로젝트 세션 ID·재시도 상한·비용/사용량 제한·명시적 중지 기능을 사용한다. 이 문서 묶음에 자동 실행기가 이미 설치되어 있는 것은 아니다. [공식 비대화형 실행 문서](https://learn.chatgpt.com/docs/non-interactive-mode)

`AGENTS.md`에는 반복해서 적용할 짧은 규칙을, `PROJECT_BRIEF_KO.md`에는 상세 요구사항을 담았다. Codex가 프로젝트 지침을 읽는 방식은 [공식 AGENTS.md 문서](https://learn.chatgpt.com/docs/agent-configuration/agents-md)를 참고한다.
