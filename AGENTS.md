# JustVerify 작업 규칙

## 프로젝트 목표

- `PROJECT_BRIEF_KO.md`를 먼저 읽는다. 목표는 Bitcoin Core·Tor·electrs를 묶은, 이미지 설치가 쉬운 개인용 Bitcoin 노드다.
- PC통신 ANSI 스타일의 실제 TUI와 브라우저 접근을 제공한다. 메인 화면에는 차트·스파크라인·큰 그래픽을 넣지 않는다.
- 한 화면에는 핵심 요약을 모으고 설정·전체 피어 목록·긴 로그·QR은 팝업 또는 상세 화면으로 연다.
- `justverify.local`, 모바일 연결, Core 22부터 최신 안정판까지의 버전 선택을 최종 범위에 유지한다.
- 사용자에게는 한국어로 간단히 설명한다. 반복 타이핑을 줄이고 실행 가능한 결과를 만든다.

## 진행 방식

- 기존 사용자 파일과 변경 사항을 먼저 확인하고 보존한다. 독립된 개발 데이터와 regtest를 기본으로 사용한다.
- 작업 범위는 상세 요구사항의 S0→S4 전체다. 단계 완료 시 사용자에게 계속할지 묻지 않고 다음 단계로 진행한다. 첫 실행 가능한 단계부터 이미지 빌드 경로를 유지한다.
- 되돌릴 수 있는 일반 구현·문서·테스트 작업은 직접 진행한다. 중요한 가정과 기술 선택은 `docs/DECISIONS.md`에 기록한다.
- 진행 알림은 짧게 하고 답변을 요구하지 않는다. 기술 선택은 문서의 기본안과 제품 목표로 결정한다.
- `docs/STATUS.md`에 완료 항목, 실행한 검증, 미검증 항목, 다음 작업을 남겨 새 대화에서도 이어갈 수 있게 한다.
- 실행 환경·도구·권한·시험 장비는 직접 점검한다. 하나의 장애로 전체 작업을 중단하지 말고 의존하지 않는 구현·검증을 계속한다.
- 빌드·통합·설치 시험에 실패하면 원인을 분석하고 수정·재검증한다. 같은 실패를 새로운 근거 없이 무한 재시도하지 않는다.
- 테스트 삭제, mock 대체, 기준 완화, 지원 범위의 묵시적 축소로 완료 판정을 만들지 않는다. 통과한 검증은 변경 영향이나 남은 위험이 있을 때만 반복한다.
- 새로운 요구사항 없이 범위를 앱스토어·Lightning·탐색기·차트·자체 합의 구현으로 넓히지 않는다.
- 실제 장치 포맷, 기존 노드 데이터 삭제·이전, 파괴적 복구 및 공개 배포는 사용자가 허용한 대상·범위에서만 진행한다. 이미 허용된 범위는 다시 승인받지 않는다. 새 승인이 필요하면 준비와 비파괴 검증을 먼저 완료한다.
- 사용자만 해결할 수 있는 접근권한·인증·장비·비용 문제는 해결 시도와 최소 필요 조치를 묶어 알린다. 허용되지 않은 작업이나 접근 통제를 우회하지 않는다.
- 실행이 중단될 수 있으므로 코드와 재개 상태를 저장한다. 실제 실행기가 없는 상태에서 자동으로 다시 시작한다고 주장하지 않는다.

## 버전과 설정

- Core·electrs·OS·라이브러리의 버전 및 출처를 고정하고 기록한다. 오래된 지식으로 현재 버전 번호를 추정하지 않는다.
- 버전별 실행 파일의 `-help`, `-help-debug`, 공식 소스와 릴리스 노트를 근거로 설정 카탈로그를 만든다.
- 특정 Core 버전에 없는 옵션, 삭제되었거나 무시되는 옵션, 지원하지 않는 네트워크를 적용하지 않는다.
- Core 버전, electrs 버전, RPC 형식, 데이터 형식, mempool 옵션을 함께 검증한다. 버전 문자열만 바꾸는 기능으로 끝내지 않는다.
- 이전 버전으로의 데이터 호환성을 추정하지 않는다. 검증되지 않은 조합은 기존 데이터를 그대로 열지 않고 가능한 별도 데이터 경로를 안내한다.
- Core 바이너리는 공식 체크섬과 신뢰한 서명자의 서명으로 검증한다. 서명 검증 실패를 우회하지 않는다.
- 정책은 로컬 mempool 수용·전파 정책과 합의 규칙을 구분한다. 제공하지 않는 필터를 제공한다고 표시하지 않는다.
- 입력 수수료 단위와 Core 설정 단위를 명시한다. 부동소수점 오차 없이 변환한다.

## 데이터와 보안 경계

- TUI는 비권한 사용자로 실행한다. 관리 작업은 명시적으로 허용된 좁은 API로 처리한다.
- 브라우저에는 고정된 TUI 프로그램만 제공한다. 임의 셸, root 셸, 프로세스 종료 후 셸 복귀를 제공하지 않는다.
- RPC는 기본적으로 로컬에 한정한다. 외부 연결은 개별 인증, 전송 보호, 접근 제한을 갖춘 경로로 제공한다.
- Tor 주소만으로 RPC가 안전해졌다고 판단하지 않는다. 공개 접근 가능한 onion 뒤에 무제한 Core RPC를 그대로 노출하지 않는다.
- RPC 자격증명, 세션 토큰, 쿠키, Tor 개인키를 로그·스크린샷·예제·Git·배포 이미지에 넣지 않는다.
- peer 문자열·로그·RPC 문자열의 ANSI/OSC 제어문자를 정화하여 터미널 및 브라우저에 표시한다.
- 기본 모드는 개인키를 보관하지 않는 노드다. RPC 지갑 연동에 필요한 경우 별도 watch-only 프로필을 구현한다.
- 이미지에 공통 암호, SSH 호스트키, Tor 개인키, machine-id 또는 기기별 인증서를 미리 굽지 않는다.
- 설정 변경은 검증·변경 내용 표시·원자적 저장·필요 서비스 재시작·상태 확인 순서로 한다. 데이터 마이그레이션 뒤 바이너리만 되돌리는 자동 복구는 금지한다.

## 출처와 검증

- Umbrel은 기능·설정 대응표의 참고자료로 사용한다. 재사용할 소스·리소스는 해당 파일의 라이선스를 확인하고 고지 의무를 지킨다.
- 실행하지 않은 검증은 `NOT RUN`, 장비나 환경이 없어 못한 검증은 `BLOCKED`로 기록한다. mock 데이터나 스크린샷으로 통합 성공을 주장하지 않는다.
- 의미 있는 검증에 집중한다: 설정 오적용, 데이터 손상, 버전 호환, 인증·비밀정보 노출, 서비스 복구, QR 인식, 실제 이미지 부팅.
- 코드 작성, 빌드 성공, VM 부팅, 대상 하드웨어 부팅, 실제 지갑 연결 및 배포 준비 완료를 각각 구분한다.
- `docs/ACCEPTANCE.md`에 요구사항별 구현 위치, 검증 명령, 대상 버전·장비, 결과와 증거 경로를 연결한다. 충족하지 못한 필수 항목이 있으면 전체 완료라고 보고하지 않는다.
- 핵심 사용자 흐름에 TODO·미동작 메뉴·가짜 데이터가 남아 있지 않은지 최종 검토하고 수정한다.
- 릴리스에는 테스트 결과, 지원 조합, 미지원 조합, 체크섬, 서명, 설치·복구 안내를 포함한다.
- 동작 버전을 전달할 때 실행 방법과 현재 한계를 함께 설명한다.
