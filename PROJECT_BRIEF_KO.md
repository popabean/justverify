# JustVerify — Codex 개발 요구사항

작성일: 2026-09-11

개정: 중간 단계별 사용자 승인 없이 S0부터 S4까지 연속 수행하고 실제 검증 기준으로 완료를 판정한다.

이 문서는 구현 지시서다. 부팅 이미지와 실행 코드가 완성되었다는 보고서가 아니다. 아래 기술 구성은 제안한 기본안이고, 제품 요구사항은 사용자가 요청한 목표다.

## 1. 제품 목표와 우선순위

JustVerify는 개인용 Bitcoin 노드 배포판이다. 사용자는 이미지 파일을 저장장치에 기록하고 전원을 켠 뒤 `justverify.local`에서 초기 설정을 완료할 수 있어야 한다. 일반 설치·운영 과정에 Linux 명령어 입력을 요구하지 않는 것이 최우선이다.

화면은 옛 PC통신의 ANSI TUI 느낌으로 만든다. Umbrel 홈의 기기 정보와 Bitcoin Node 앱의 핵심 정보를 한 화면에 응축한다. 차트, 스파크라인, 큰 카드, 장식용 그래픽은 사용하지 않는다. 수치·짧은 상태 문구·작은 표·구분선·단축키를 사용한다.

| 우선순위 | 최종 요구사항 |
|---|---|
| 1 | 기록 가능한 설치 이미지, 쉬운 초기 설정, 재부팅 후 자동 복구 |
| 2 | 차트 없는 고밀도 통합 TUI와 `justverify.local` 접속 |
| 3 | Bitcoin Core, Tor, electrs의 통합 설치·설정·상태 확인 |
| 4 | 모바일 지갑용 LAN·Tor 주소와 앱에 맞는 연결 QR |
| 5 | 버전별로 제공 가능한 mempool·relay 정책 설정 전체의 관리 |
| 6 | Core 22부터 최신 안정판까지 버전 선택과 데이터 보호 |

초기 릴리스에서는 앱스토어, Lightning, 웹 블록 탐색기, 실시간 차트, 가격 API, 채굴 UI, 자체 합의 엔진을 추가하지 않는다. mempool 정책 설정을 위해 별도의 mempool.space 서버가 필요한 것은 아니다.

## 2. 기본 하드웨어와 구성

기본 개발 대상은 Raspberry Pi 5, 64비트, 유선 LAN, SSD다. 기본 검증 장비는 RAM 8 GB와 NVMe 2 TB로 제안한다. 더 작은 메모리 지원 여부는 실측으로 결정한다. 저장공간 요구량은 당시 체인 크기, electrs 인덱스, 재인덱싱 임시 공간, 여유 공간을 측정하여 표시한다. UI가 가벼워도 체인 동기화와 인덱싱 자원 요구량은 남는다.

| 구성요소 | 제안한 구현 | 책임 |
|---|---|---|
| OS | Raspberry Pi OS Lite 64-bit 기반, 검증한 버전 고정 | 부팅·장치 드라이버·네트워크 |
| 프로세스 관리 | systemd 서비스 | 자동 시작·의존성·정지·재시작 |
| TUI | Rust + Ratatui | 로컬 콘솔·SSH·브라우저용 동일한 화면 |
| 관리 데몬 | Rust, 로컬 Unix socket API | 수집·검증·설정·제한된 서비스 제어 |
| 브라우저 접근 | xterm.js + 인증된 고정 TUI 프로세스 연결 | `justverify.local`에서 동일 TUI 조작 |
| Bitcoin 노드 | 공식 Bitcoin Core 바이너리 | 합의 검증·P2P·mempool·RPC |
| 지갑 인덱서 | 호환성을 검증한 electrs | Electrum 프로토콜 서비스 |
| 프라이버시 | Tor | SOCKS 경로·서비스별 onion 주소 |
| 로컬 탐색 | mDNS/Avahi | `justverify.local` 이름 광고 |

Ratatui는 Rust TUI 라이브러리이고, xterm.js는 브라우저 터미널을 제공한다. 이 조합은 제안이며, 유지보수가 더 쉬운 동등한 구현으로 바꿀 경우 이유를 기록한다. [Ratatui 공식 문서](https://ratatui.rs/), [xterm.js 공식 문서](https://xtermjs.org/)

처음에는 native 서비스로 단순하게 구성한다. 컨테이너 도입이 버전 격리나 운영에 실제로 유리하면 근거를 기록하고 선택할 수 있으나, Docker가 설치 이미지를 대체하지는 않는다. 브라우저용 정적 파일은 이미지에 포함하고 런타임 CDN에 의존하지 않는다.

향후 x86_64 미니PC용 Debian 계열 UEFI 설치 이미지를 추가한다. ARM 이미지 하나로 x86까지 지원한다고 표시하지 않는다. Mac에서는 개발할 수 있지만 Linux 서비스 및 이미지 빌드·검증 환경은 분리한다.

## 3. 메인 화면

기준 크기는 120열 × 40행이다. 이 크기에서 핵심 정보는 스크롤 없이 표시한다. 80열 × 24행은 축약 모드, 작은 모바일 화면은 요약 모드와 상세 화면을 사용한다. 모든 피어와 모든 설정을 좁은 한 화면에 넣는 것은 요구하지 않는다.

| 구역 | 표시 정보 |
|---|---|
| 상단 | JustVerify, hostname, LAN IP, 네트워크, Core 버전, 현재 시각, 중요 경고 |
| 시스템 | uptime, CPU 사용률·온도, RAM·swap, 디스크 사용·남은 공간, 가능하면 SSD 상태 |
| 체인 | block/headers 높이, 검증 진행률, IBD 여부, 마지막 블록 경과시간, 체인 크기, prune·index 상태 |
| mempool | 거래 수, 가상 크기, 실제 메모리 사용량/설정 상한, 총 수수료, 현재 mempool 최저 수수료 |
| 수수료 | 목표 블록별 추정 sat/vB, 추정 불가 상태, relay 최소 수수료와의 구분 |
| 네트워크 | inbound/outbound 수, Clearnet·Tor·I2P별 연결 수, 송수신량·속도, 관측된 연결 상태 |
| 서비스·연결 | Core·Tor·electrs 상태, electrs 인덱싱 진행률, LAN/onion 주소 요약, QR 열기 |
| 하단 | 최근 오류·설정 적용 상태·단축키 |

큰 QR은 기본 화면에서 공간을 차지하지 않게 `Q` 또는 연결 메뉴에서 연다. 화면이 넓을 때만 최근 블록 3~5개 또는 피어 요약을 추가할 수 있다. 기본 화면에 숨겨진 차트나 진행 막대를 다시 넣지 않는다.

키보드 예시: `S` 설정, `M` mempool 정책, `P` 피어, `C` 연결, `V` 버전, `L` 로그, `?` 도움말, `Esc` 뒤로. 브라우저에서 단축키가 충돌하면 화면 버튼도 제공한다. QR은 마우스 없이도 열 수 있게 한다.

TUI는 ANSI 색상과 고정폭 레이아웃을 사용하되 번쩍임과 과도한 애니메이션을 피한다. 16색·무색 모드를 제공한다. 색만으로 상태를 표현하지 않는다. 한국어와 영문 라벨의 글자 폭, 터미널 리사이즈, 브라우저 확대를 검증한다.

## 4. 실제 데이터 수집

Core의 `getblockchaininfo`, `getnetworkinfo`, `getnettotals`, `getmempoolinfo`, `getpeerinfo`, `getindexinfo`, `estimatesmartfee` 등을 버전별 지원 여부에 따라 사용한다. 없는 필드는 `N/A`, 일시 실패는 마지막 갱신 시각과 stale 상태로 표시한다. 추정치를 실제 측정값처럼 표시하지 않는다.

가벼운 상태는 약 2초, 디스크·온도 등은 5~10초 간격으로 수집하는 것을 출발점으로 삼는다. 화면을 그릴 때마다 전체 mempool이나 전체 거래를 조회하지 않는다. 비동기 수집, 타임아웃, 제한된 재시도, 공유 캐시를 사용하고 여러 브라우저 세션이 열려도 RPC 부하를 배수로 늘리지 않는다.

초기 성능 예산은 Core·electrs·Tor를 제외한 관리 구성요소 합계 RSS 150 MiB 이하, 정상 상태 평균 CPU 한 코어 기준 5% 이하를 목표로 한다. 측정 장비·세션 수·수집 주기를 명시하고 결과에 따라 조정한다. 이 값은 현재 측정 결과가 아니다.

수수료는 화면에서 sat/vB를 기본으로 하고 원본 단위를 함께 확인할 수 있게 한다. mempool의 가상 크기와 RAM 사용량을 혼동하지 않는다. 외부 피어의 user-agent, 로그, RPC 문자열은 ANSI/OSC 제어문자를 제거한 뒤 표시한다.

## 5. Umbrel 설정을 빠짐없이 대응시키는 방법

Umbrel 홈과 Bitcoin Node의 설정·연결 항목을 조사하고 `docs/UMBREL_PARITY.md`에 대응표를 만든다. 각 행에는 다음 정보를 둔다.

- 원본 기능, 원본 URL 및 고정 commit.
- JustVerify의 메뉴 위치와 사용자에게 보이는 설명.
- 실제 Core/Tor/electrs 옵션 및 변경 적용 방식.
- 적용 가능한 Core 버전과 네트워크.
- 구현 상태: 완료, 미구현, 해당 없음, 버전 제약.

조사 시작점은 [Umbrel Bitcoin 설정 메타데이터](https://github.com/getumbrel/umbrel-bitcoin/blob/2fe07948f99e101dbee95ce34e5947a69c441ee4/libs/settings/settings.meta.ts)다. 이 commit은 작성일에 확인한 참고점이며 구현을 시작할 때 최신 변경도 비교한다.

대응 범위에는 outgoing 네트워크 선택, Clearnet 목적지로의 Tor 경유, incoming 네트워크, peer 제한, 송수신 버퍼·타임아웃, 업로드 한도, dbcache, prune, txindex·blockfilter 등 인덱스, mempool·relay, RPC·REST, Core 버전·네트워크가 포함된다. I2P는 Tor와 다른 서비스이므로 미구현인데 토글만 제공하지 않는다. Tor·electrs를 먼저 완성하고 I2P는 대응표에 상태를 명시한 별도 단계로 추가할 수 있다.

Umbrel과 umbrel-bitcoin의 현재 라이선스는 PolyForm Noncommercial이다. 기능 구성은 참고하되 구현은 기본적으로 독립 작성한다. 실제 코드를 재사용하면 해당 파일과 의존성의 조건 및 고지를 확인하고 `THIRD_PARTY_NOTICES.md`에 기록한다. 복사한 코드까지 아무 제약 없는 자체 MIT 코드라고 표시하지 않는다. [Umbrel 라이선스](https://github.com/getumbrel/umbrel/blob/master/LICENSE.md), [Bitcoin Node 앱 라이선스](https://github.com/getumbrel/umbrel-bitcoin/blob/master/LICENSE.md)

## 6. Mempool·relay 정책 전체 설정

여기서 “전체”는 선택한 공식 Core 버전이 실제로 노출하는 관련 설정 전체다. 내부 상수나 설정 인터페이스가 없는 정책을 가상의 토글로 만들지 않는다. 그런 항목은 현재 동작과 설정 불가 사유를 읽기 전용으로 설명한다. 사용자 요청 없이 Core를 패치하지 않는다.

각 지원 버전의 `bitcoind -help`, `-help-debug`, 공식 소스의 옵션 등록·기본값·범위, 릴리스 노트를 대조한 카탈로그를 만든다. 공개 도움말 밖의 설정은 고급 설정으로 분류하고 근거와 적용 범위를 명확히 한다. 추가·삭제·동작 변경된 옵션은 자동 비교하여 검토 대상으로 표시한다. 런타임의 도움말 파싱만으로 전체 의미가 검증되었다고 판단하지 않는다.

| 설정 범주 | 조사할 옵션·동작의 예시 — 모든 버전에 공통이라는 뜻은 아님 |
|---|---|
| 용량·보존 | `maxmempool`, `mempoolexpiry`, `persistmempool` |
| 수수료·dust | `minrelaytxfee`, `incrementalrelayfee`, `dustrelayfee` |
| 데이터·표준성 | `datacarrier`, `datacarriersize`, `permitbaremultisig`, 네트워크별 표준성 제약 |
| 거래 관계 | 버전별 ancestor/descendant 제한 또는 cluster 크기·개수 제한 |
| 교체·패키지 | 해당 버전의 RBF·package·TRUC 정책, 실제 설정 가능한 부분과 고정된 부분 |
| orphan·relay | 버전별 orphan 관련 설정, `blocksonly` 등 관련 동작 |
| 블록 템플릿 | `blockmintxfee` 등 — 내 노드의 수용·전파 정책과 분리 표시 |

카탈로그의 각 항목에는 최소한 `core_version`, `key`, `source`, `type`, `unit`, `default`, `range`, `network_scope`, `introduced/removed/changed`, `restart_required`, `dependencies`, `conflicts`, `verification_method`를 기록한다.

화면에는 기본값, 사용자가 요청한 값, 저장된 값, 확인 가능한 적용값을 구분한다. Core RPC가 모든 설정의 실효값을 제공하는 것은 아니므로 RPC로 읽지 못한 값을 “RPC로 검증 완료”라고 표시하지 않는다. 설정 생성·시작 로그·행동 검증 중 어떤 근거를 사용했는지 남긴다.

설정 변경 순서는 입력 검증 → 단위 변환 → 충돌 확인 → 변경 내용과 재시작 영향 표시 → 원자적 저장 → 필요한 서비스만 재시작 → 상태 확인이다. 단순 설정 실패의 복구와 데이터 마이그레이션 뒤의 복구를 구분한다. 사용자 지정 설정도 제공하되 관리 설정과 중복되면 우선순위를 명시하고 버전에서 지원하지 않는 옵션을 거부한다.

정책 기본값은 선택한 Core의 기본값을 출발점으로 한다. 자원·네트워크 관련 JustVerify 재정의 값은 명시한다. 임의의 스팸 필터 프리셋을 기본 활성화하지 않는다. OP_RETURN 설정만으로 Ordinals·Runes 등 모든 데이터 거래가 차단된다고 설명하지 않는다. 로컬 정책에서 거부한 거래가 유효한 블록에 들어왔을 때의 합의 검증은 별개다.

현재 확인된 변경 예: Core 30은 `datacarriersize` 기본값과 relay 관련 최소 수수료 기본값을 변경했다. Core 31은 ancestor/descendant 제한을 cluster 제한으로 대체하고 `maxorphantx`를 제거했다. 따라서 전체 버전에 동일한 설정 양식을 적용하면 안 된다. [Core 30 릴리스 노트](https://bitcoincore.org/en/releases/30.0/), [Core 31 릴리스 노트](https://bitcoincore.org/en/releases/31.0/)

## 7. Core 22부터 최신 안정판까지의 선택

2026-09-11 확인 기준 공식 다운로드 페이지의 최신 안정판은 31.1이다. 구현에서는 상한을 코드에 고정하지 말고 검증한 릴리스 카탈로그를 갱신할 수 있게 한다. 기본 실행 버전은 릴리스 시점에 검증한 최신 안정판으로 고정하며, 사용자가 선택한 버전을 예고 없이 변경하지 않는다. [공식 다운로드](https://bitcoincore.org/en/download/), [공식 릴리스 목록](https://bitcoincore.org/en/releases/)

최종 선택 범위는 22.x부터 최신 안정판까지 공식 배포된 안정 릴리스다. 기본 목록에는 각 major의 최신 patch를 보여주고 확장 목록에서 이전 patch를 볼 수 있게 한다. RC·개발 브랜치는 기본 목록에서 제외한다. 공식 바이너리가 없는 대상이나 검증 실패 조합에는 이유를 표시한다.

오래된 버전은 선택 기능을 제공하되 upstream 지원 상태와 알려진 제한을 표시한다. 과거 버전을 제공한다는 것과 그 버전에 보안 업데이트가 제공된다는 것은 다르다. Core 31 릴리스 노트는 28.x 이하의 EOL을 알리고 있다. [공식 지원 상태 안내](https://bitcoincore.org/en/releases/31.0/)

카탈로그에는 버전, architecture, 다운로드 출처, SHA256, 검증한 서명자 fingerprint, 릴리스 날짜, 지원 상태, electrs 조합, 설정 스키마, RPC 차이, 데이터 호환성, 테스트 결과를 기록한다. 모든 버전을 기본 이미지에 포함할 필요는 없다. 기본 한 버전을 포함하고 다른 버전은 사용자가 선택하면 검증 후 설치·캐시한다.

버전 전환은 다음 절차를 구현한다.

1. 현재/목표 버전, OS·CPU, 설정 차이, RPC 차이, electrs 조합, 데이터 형식을 사전 검사한다.
2. 다운로드·서명 검증과 필요한 공간 확인을 먼저 완료한다.
3. 중단 시간, 재인덱싱·재동기화 가능성, 되돌리기 가능한 범위를 사용자에게 표시한다.
4. 영향받는 서비스를 정상 종료하고 일관된 복구 지점을 확보한다.
5. 검증한 조합만 실행하고 Core·electrs·TUI·연결 상태를 확인한다.
6. 복구는 데이터와 실행 파일의 호환성이 확인된 경우에만 수행한다.

“22↔최신 버전 버튼을 누르면 같은 데이터로 즉시 전환”을 보장하지 않는다. 검증되지 않은 다운그레이드에는 별도 데이터 디렉터리, 이전에 정지 상태에서 만든 호환 스냅샷 또는 새로운 동기화 경로를 제공한다. 구버전 선택 자체를 없애는 대신 해당 버전으로 안전하게 시작하는 경로를 구현한다.

특히 block 파일·block index·chainstate·mempool.dat·wallet·electrs 인덱스를 각각 검토한다. Core 28에는 비영(非零) 키로 XOR 처리된 block 파일을 이전 버전이 읽지 못하는 변경이 있다. 단순 `-reindex`나 실행 파일 교체가 해결책이라고 가정하지 않는다. 모든 버전마다 전체 체인을 복제하지도 말고, 검증된 재사용 방식과 필요한 추가 공간을 표시한다. [Core 28 저장 형식 변경](https://bitcoincore.org/en/releases/28.0/)

## 8. electrs 호환성

electrs는 Electrum 지갑과 구분되는 서버다. Core의 상태와 별도로 인덱싱·준비·오류 상태를 표시한다. Core가 동기화되었더라도 electrs가 준비되지 않았으면 지갑 연결 준비 완료로 표시하지 않는다.

최신 electrs 하나가 Core 22부터 모두 지원한다고 가정하지 않는다. 작성일 확인 기준 현재 electrs 사용 안내는 Core 31 이상을 전제로 하고, 업그레이드 문서는 0.12.0의 새로운 REST API 의존성과 인덱스 형식 변경을 설명한다. 반면 v0.10.10 사용 안내는 Core 0.21 이상을 전제로 한다. 이는 구버전 조합을 조사할 근거이며, 실제 모든 조합의 통합 테스트를 대신하지 않는다. [현재 사용 안내](https://github.com/romanz/electrs/blob/master/doc/usage.md), [업그레이드 안내](https://github.com/romanz/electrs/blob/master/doc/upgrading.md), [v0.10.10 사용 안내](https://github.com/romanz/electrs/blob/v0.10.10/doc/usage.md)

Core·electrs의 검증된 조합을 manifest로 관리한다. 인덱스 형식이 달라지면 버전별 인덱스 경로 또는 재인덱싱을 사용하고 기존 인덱스를 묵시적으로 파괴하지 않는다. REST가 필요한 버전에서는 로컬 인덱서에 필요한 경로만 제공하고 외부 공개와 혼동하지 않는다.

기본 electrs 프로필은 prune=0의 전체 블록 보관을 사용한다. prune 프로필을 선택하면 해당 인덱서의 지원 여부를 검증하고 충돌 시 electrs 비활성화 등의 선택을 제공한다. electrs만을 이유로 txindex를 필수 활성화하지 않는다. 다른 클라이언트 기능이 요구하면 그 이유를 표시한다. [electrs 공식 README](https://github.com/romanz/electrs/blob/master/README.md)

## 9. 네트워크·Tor·주소

`justverify.local`은 LAN에서 찾는 이름이다. 이름만 설정한다고 웹 화면이 생기지 않으므로 mDNS와 실제 브라우저 서비스를 함께 구성한다. 기본 hostname은 `justverify`, 충돌 시 실제 사용 중인 이름을 표시하고 변경할 수 있게 한다. mDNS가 차단된 환경이나 지원하지 않는 클라이언트에는 LAN IP로 접근하는 대안을 제공한다.

다음 세 설정을 서로 분리한다: outgoing 목적지 네트워크 선택, Clearnet 목적지 연결의 Tor 경유 여부, incoming 네트워크 선택. Tor 장애 시 Clearnet으로 자동 우회할지는 명시적인 정책으로 정하고 Tor-only에서는 우회하지 않는다. 다운로드·DNS 등 다른 트래픽까지 Tor로 보호된다고 주장하려면 그 범위도 검증한다.

P2P, Electrum, RPC, 관리 화면은 각기 다른 서비스다. 서비스별 LAN 주소, onion 주소, 포트, 활성화 여부, 연결 상태를 제공한다. Tor onion 개인키는 첫 부팅에 생성하고 업데이트 후 유지한다. 기본 구성에서는 서비스별 onion을 분리한다. [Tor onion 서비스 구성 문서](https://community.torproject.org/onion-services/setup/)

inbound 활성화와 실제 외부 도달 가능성을 구분한다. Clearnet은 공유기·방화벽·CGNAT의 영향을 받으며 설정 토글만으로 수신 연결이 보장되지 않는다. 관측된 inbound 수와 마지막 관측 시간을 보여준다. reachability 미검증이면 미검증으로 표시한다.

기본 방화벽은 관리 서비스의 WAN 공개와 공개 RPC를 차단한다. 내부 네트워크에 필요한 포트만 열고 UPnP 등 자동 포트 개방 여부를 사용자에게 숨기지 않는다. I2P를 추가하면 별도 router/SAM 설정과 서비스 상태를 관리한다.

## 10. 모바일 지갑 연결과 QR

초기 검증 대상으로 Electrum 서버를 사용하는 Nunchuk과 Core RPC를 사용하는 Fully Noded를 제안한다. 대상 앱의 실제 버전·플랫폼과 공식 입력 형식을 확인한다. 앱별 자동 가져오기 지원 여부를 확인하기 전에는 QR을 찍으면 모두 연결된다고 주장하지 않는다.

연결 화면에서 서비스와 앱을 선택하면 호스트, 포트, 프로토콜, TLS 여부, LAN/Tor 구분, 필요한 프록시, 자격증명 유무와 실제 QR을 표시한다. Electrum 연결 문자열을 HTTP 웹 주소라고 부르거나 Core RPC URI를 Electrum QR로 보내지 않는다. P2P 8333과 RPC 8332도 구분한다.

일반 QR은 정확한 host/port 또는 앱이 요구하는 텍스트를 담는다. 해당 앱이 공식적으로 지원하는 가져오기 URI/JSON이 있으면 그 형식으로 별도 생성한다. 자동 가져오기가 없으면 주소 복사와 수동 입력 안내를 제공하고 제한을 명시한다. onion 접속에 필요한 Tor/프록시 구성은 앱·OS별로 확인한다.

TUI QR은 충분한 quiet zone, 올바른 모듈 비율, 높은 대비를 확보하고 실제 휴대폰으로 스캔한다. 브라우저에서는 동일 payload의 선명한 QR 보기와 복사를 제공할 수 있다. 긴 onion 주소와 인증정보가 포함된 payload도 검증하며, QR이 잘리는 경우 별도 확대 화면을 사용한다.

RPC는 내부 cookie 인증을 우선한다. 외부 앱용 자격증명은 기기/앱별 생성·폐기·교체를 지원하고, 외부 클라이언트에 내부 cookie를 전달하지 않는다. RPC 비밀정보 QR은 인증된 사용자가 연결 화면에서 명시적으로 열 때만 표시하고 기본 화면·로그·진단자료에는 포함하지 않는다.

Core는 RPC의 인터넷 직접 노출을 권장하지 않으며 Tor onion 경유만으로도 그 경고가 사라지지 않는다. 모바일용 원격 RPC는 Core 앞의 인증·접근 제한·요청 크기 및 빈도 제한·필요 메서드 제한 또는 지원되는 Tor client authorization 등으로 보호한다. 메서드 제한은 임의 상수가 아니라 목표 앱의 동작 테스트로 확인한다. LAN 원격 RPC에도 TLS/VPN/SSH 등 전송 보호와 실제 앱 호환성을 확인한다. [Bitcoin Core RPC 보안 및 버전 문서](https://github.com/bitcoin/bitcoin/blob/v31.1/doc/JSON-RPC-interface.md)

기본 노드 프로필은 개인키를 저장하지 않는다. RPC 앱이 wallet RPC를 요구할 때는 watch-only descriptor 지갑 프로필을 별도로 제공한다. 노드 화면에서 seed나 개인키 입력을 요청하지 않는다.

## 11. 브라우저 접근과 첫 신뢰 설정

웹 터미널은 인증된 세션에 고정된 JustVerify TUI만 연결한다. 셸 명령을 URL·WebSocket 입력으로 받지 않는다. TUI가 종료되면 연결을 닫고 셸로 떨어지지 않는다. 관리 데몬의 제한된 API를 통해서만 설정을 변경한다.

세션 만료·로그아웃·Origin 검증·CSRF 방어·로그인 시도 제한·민감 작업 재인증을 설계한다. 브라우저가 가진 관리 세션을 Core RPC 자격증명으로 사용하지 않는다. 기본 외부 접근은 비활성화하고 LAN 접근을 우선한다.

`https://justverify.local`에 공인 인증서가 자동으로 발급된다고 가정하지 않는다. 장치별 인증서와 최초 신뢰 확인 절차, 또는 동등하게 보호된 접근 방식을 실제 지원 브라우저에서 검증한다. 첫 연결에는 인증서 신뢰 안내 등 추가 단계가 필요할 수 있으며 사용 안내에 명시한다. 평문 HTTP로 관리자 암호·RPC 비밀정보를 전송하는 방식으로 편의성을 해결하지 않는다.

처음 설치하는 사용자를 위해 로컬 콘솔 표시 또는 이미지 기록 전 설정 파일 등으로 기기 소유자가 일회성 초기 설정 정보를 얻는 방법을 제공한다. 공통 기본 암호와 LAN의 첫 접속자에게 무조건 소유권을 주는 방식은 사용하지 않는다. 모니터 없는 설치 경로와 모니터가 있는 설치 경로를 각각 검증한다.

## 12. 배포 이미지와 설치 흐름

첫 단계부터 장치별 부팅 이미지를 자동으로 만드는 빌드 스크립트를 둔다. 파일을 수동으로 복사해 만든 개발자 개인 디스크 복제본을 최종 배포물로 삼지 않는다. OS 기반 이미지·패키지·도구·소스 버전과 빌드 환경을 고정하고 재현 가능성을 점검한다. 바이트 단위 재현성을 달성하지 못하면 달성했다고 표시하지 말고 차이를 기록한다.

Raspberry Pi 배포물 예시는 `justverify-<version>-rpi5-arm64.img.xz`, x86 배포물은 별도의 UEFI installer 이미지다. 이미지 기록 도구에서 사용자 지정 이미지로 선택하는 경로를 문서화한다. 특정 도구가 해당 압축형식을 지원하는지 확인하고 필요하면 압축 해제 단계를 안내한다. [Raspberry Pi 공식 설치 안내](https://www.raspberrypi.com/documentation/computers/getting-started.html)

설치 목표 흐름은 이미지 기록 → LAN·전원 연결 → `justverify.local` 접속 → 소유권·관리 계정 설정 → 데이터 디스크 확인 → 네트워크·노드 버전 선택 → 동기화 시작이다. 필요한 다운로드 크기와 최초 동기화는 숨기지 않는다. 메인 화면 접근을 체인 전체 동기화가 끝날 때까지 막지 않는다.

NVMe 직접 부팅과 microSD 부팅 + SSD 데이터의 지원 여부를 명시한다. NVMe 직접 부팅은 대상 Pi의 EEPROM·bootloader·HAT 조합으로 검증한다. 모든 Raspberry Pi에 같은 이미지가 된다고 가정하지 않는다.

첫 부팅에는 다음 작업을 자동화한다.

- 장치별 machine-id, SSH 호스트키, 인증서, Tor 개인키와 자격증명 생성.
- OS 파일시스템 확장과 데이터 디스크 탐색. 별도 기존 디스크는 자동 포맷하지 않음.
- hostname·mDNS·시간 동기화·네트워크·서비스 의존성 구성.
- 데이터 마운트가 없을 때 시스템 디스크로 체인 데이터를 대신 쓰지 않도록 차단.
- 중단 후 재실행해도 안전한 초기 설정과 명확한 오류 표시.

OS·애플리케이션과 블록체인 데이터를 분리한다. 시스템 업데이트는 검증된 artifact를 원자적으로 적용하고 last-known-good 시스템으로 복구할 수 있게 한다. 이것이 Core/electrs 데이터 마이그레이션까지 되돌린다는 뜻은 아니다. OS, 앱, Core, 인덱서는 각자의 호환성과 업데이트 정책을 가진다.

설정·Tor identity·연결 정보의 암호화 백업과 복구 경로를 제공한다. 기본 백업에 전체 체인을 포함하지 않는다. Tor identity를 유지한 복구와 새 주소를 생성하는 재설치를 구분한다. 공장 초기화 시 삭제되는 범위를 명확히 표시한다.

최종 산출물에는 이미지, SHA256SUMS, 프로젝트 릴리스 서명, 검증용 공개키 안내, 버전 manifest, 설치·업데이트·복구 문서, 테스트 보고서, 제3자 라이선스 고지를 포함한다. 실험용 서명키는 실험용이라고 표시하고 배포용 개인 서명키를 저장소에 넣지 않는다.

## 13. 개발 단계와 완료 기준

| 단계 | 실제 산출물 | 완료 확인 |
|---|---|---|
| S0 — 기반 설계 | 저장소 구조, 요구사항 대응표, 위협·데이터 경계, 버전 카탈로그 초안, 빌드 구조 | 모호한 부분을 기본안으로 해결하고 결정·미검증 사항 기록 |
| S1 — 첫 동작 버전 | 실제 regtest Core에 연결된 TUI·기기 정보·브라우저 접근의 동작 버전, 최소 이미지 빌드 경로 | 숫자가 실제 RPC로 갱신되고 서비스 중단을 표시; 가능한 Linux/VM에서 실행 |
| S2 — 연결 기능 | Tor·electrs, LAN/onion 연결 화면, QR, 초기 설정 | 인덱싱·서비스 재시작·앱 연결 흐름 검증 |
| S3 — 설정과 버전 | Umbrel 대응 설정, 전체 정책 카탈로그, Core 22~최신 선택, 데이터 보호 | 지원 조합 matrix, 잘못된 옵션 거부, 수수료 단위 및 버전 전환 검증 |
| S4 — 배포 완성 | 최종 장치 이미지, 업데이트·복구·서명·문서 | 깨끗한 장치 설치, 재부팅·오류 복구, 실제 지갑 QR 연결 |

개발은 S0부터 S4까지 연속 수행한다. 단계의 구분은 구현 순서와 검증 기준이며 사용자 승인 대기 지점이 아니다. S1에서 이미지 구조를 만들고 이후 매 단계 갱신한다. 화면 목업만 완성한 상태를 노드 완성으로 보고하지 않는다. 실행 환경이 부족하면 영향받지 않는 작업을 계속하고 정확히 무엇을 어디서 실행해야 하는지 남긴다.

저장소에 유지할 주요 문서: `docs/STATUS.md`, `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`, `docs/UMBREL_PARITY.md`, `docs/VERSION_COMPATIBILITY.md`, `docs/MOBILE_CONNECTIONS.md`, `docs/INSTALL.md`, `docs/RECOVERY.md`, `docs/TEST_RESULTS.md`, `docs/ACCEPTANCE.md`.

## 14. 의미 있는 검증과 릴리스 조건

각 검증은 실제 명령, 대상 버전·하드웨어, 실행 시각, 결과와 로그 위치를 기록한다. 기본 자동화는 regtest와 격리된 Linux 환경에서 수행하고 사용자 실사용 노드로 시험하지 않는다.

| 검증 영역 | 필요한 증거 |
|---|---|
| Core 버전 | 최종 지원한다고 표시한 각 릴리스·architecture의 다운로드·검증·기동·RPC smoke test |
| 설정 | 버전별 허용·금지 옵션, 범위·단위, 충돌, 재시작 적용, 명시한 정책의 실제 수용·거부 결과 |
| 프로토콜 | 구버전 RPC 응답과 현대 RPC의 처리 차이, missing field·timeout·stale 표시 |
| Core/electrs | 조합별 인덱싱, scripthash 조회, 테스트 거래 인지·전파, 재시작 후 복구 |
| 버전 전환 | 허용된 전환, 호환되지 않는 다운그레이드 차단과 별도 실행 경로, 중간 실패 |
| 이미지 | 새 저장장치 기록, 첫 부팅, mDNS/IP 대체, 초기 설정, 서비스 자동 시작 |
| 복구 | 디스크 부족·마운트 실패·네트워크 단절·Tor 중단·Core/electrs 재시작·업데이트 실패 |
| 접근 제어 | 비인증 접근 차단, 웹 터미널 셸 탈출 방지, RPC 노출·비밀정보 로그 검사 |
| 화면·QR | 기준 터미널 크기, 리사이즈, 브라우저, 긴 payload, 실제 대상 휴대폰 앱 |
| 자원 | 정상 동기화 후 관리 구성요소 오버헤드, IBD·electrs 초기 인덱싱 시 메모리·디스크 압박 |

매 커밋마다 모든 역사 버전의 mainnet 전체 동기화를 반복할 필요는 없다. 변경 위험에 맞춰 자동화 범위를 정하고, 최종 지원 목록에 대한 실제 실행 증거와 미검증 범위를 공개한다. 장애·재인덱싱·대규모 데이터 형식의 위험은 작은 regtest 성공만으로 해소되었다고 판단하지 않는다.

릴리스 준비 완료 판정에는 실제 Raspberry Pi 5 이미지 부팅, 재부팅 후 자동 복구, 대상 모바일 지갑 연결을 포함한다. 장비가 없어 못한 검증은 `BLOCKED`, 단순 미실행은 `NOT RUN`이다. 테스트 통과율을 근거 없이 “정상동작 확률 99%”로 바꾸지 않는다. 공개 배포 전에 통과한 범위와 남은 제한을 한 페이지로 정리한다.

## 15. 사용자 개입을 최소화한 연속 수행

이번 작업의 목표는 사용자가 중간에 설계 선택, 오류 분석, 다음 단계 지시를 반복하지 않아도 최종 배포 후보까지 도달하는 것이다. `S0만`, `S1까지`를 최종 범위로 해석하지 않는다. 개발 및 배포 준비는 연속 수행하며, 실제 공개 배포는 허용된 대상·범위에 따른다.

### 직접 결정하는 사항

프로젝트 문서의 기본안으로 충분한 라이브러리 선택, 폴더 구조, 화면 세부 배치, 내부 API, 테스트 도구, 오류 수정은 직접 결정한다. 우선순위는 요구 기능 충족 → 데이터 보존·보안 → 쉬운 설치 → 유지보수성 → 자원 효율이다. 개인 취향에 가까운 선택 때문에 작업을 멈추지 않는다.

실제 실행 권한은 별도다. 프로젝트 파일 쓰기와 지정된 시험 환경에서의 검증은 허용 범위 내에서 수행한다. 권한 없는 서버 접속, 인증 우회, 미지정 실제 디스크 포맷, 새로운 유료 리소스 사용, 미승인 공개 배포는 “자율 수행”이라는 표현으로 허용된 것으로 간주하지 않는다. 이미 사용자가 허용한 구체적인 작업은 다시 묻지 않는다.

### 작업 시작 시 환경 점검

Codex는 아래 내용을 도구로 먼저 확인하여 `docs/ENVIRONMENT.md`에 기록한다. 사용자에게 빈 양식을 작성시키는 것으로 시작하지 않는다.

| 점검 대상 | 확인 내용 | 부족할 때 진행할 수 있는 작업 |
|---|---|---|
| 개발 환경 | OS·CPU·도구 버전·프로젝트 쓰기 권한 | 문서·코드·일부 단위 검증 |
| Linux 실행 환경 | 서비스 실행·VM·가상 디스크·이미지 빌드 가능 여부 | 가능한 교차 빌드·정적 검토 |
| 다운로드 | 공식 Core·의존성 출처 접근과 서명 검증 | 이미 검증한 캐시와 독립된 작업 |
| 시험 장비 | Pi 모델·접속 방식·재부팅 후 재접속 | VM에서 가능한 시험과 장비용 실행 절차 |
| 저장장치 | OS·시험 데이터 디스크 구분, 고유 식별자, 삭제 허용 범위 | 프로젝트 내 가상 디스크만 사용 |
| 모바일 | 대상 앱·OS·실제 카메라/앱 조작 가능 여부 | payload·프로토콜·렌더링 자동 검증 |

실기 시험까지 사용자 개입을 줄이려면 시험 장치에 원격 접근할 수 있어야 한다. 이미지 최초 기록이나 부팅 실패 복구를 무인으로 수행하려면 별도의 제어·복구 경로도 준비되어야 한다. 이것이 없으면 Codex가 USB/NVMe를 물리적으로 교체하거나 휴대폰 카메라를 움직일 수 있다고 가정하지 않는다.

### 구현과 수정의 반복

1. 요구사항을 실행 가능한 작업과 검증 조건으로 나누고 전체 목록을 유지한다.
2. 실제 동작하는 작은 통합 단위부터 구현하고 검증한다.
3. 실패가 발생하면 재현 조건·로그·원인 가설을 기록하고 수정한다.
4. 수정한 부분과 영향을 받는 검증을 다시 실행한다.
5. 통과한 단계 다음으로 진행한다. 사용자에게 다음 단계를 진행할지 묻지 않는다.
6. 모든 필수 기능의 구현 후 사용자 시나리오 전체를 다시 검토하고 누락을 수정한다.

같은 오류를 변경 없이 반복 실행하지 않는다. 외부 서비스의 일시 장애는 적절한 재시도 제한을 두고, 기술적 접근이 실패하면 공식 자료와 관측 결과를 바탕으로 대안을 선택한다. 검증 결과를 좋게 보이도록 테스트를 삭제하거나 필수 기능을 제외하지 않는다.

24시간 동기화나 장시간 인덱싱이 필요하면 실제 작업 프로세스와 로그를 관리한다. 단순 대기만 하며 다른 작업을 멈추지 않는다. 사용 환경이 종료되면 프로세스도 중단될 수 있으므로 생존 여부·체크포인트·재개 방법을 확인한다. 중단 후 자동 재개는 실제 실행기가 구성된 경우에만 제공한다고 설명한다.

### 완료를 판정하는 증거

`docs/ACCEPTANCE.md`에는 요구사항 ID, 구현 위치, 자동/실기 검증 명령, 대상 버전·장비, 실행한 commit 또는 artifact hash, 결과와 증거 경로를 연결한다. 문서에 체크 표시를 한 것만으로 완료가 되지 않는다.

최종 통과에는 다음 시나리오를 포함한다.

- 깨끗한 저장장치에서 이미지 부팅, 최초 설정, `justverify.local` 및 IP 대체 접속.
- 실제 노드 데이터를 표시하는 차트 없는 TUI와 브라우저 접근.
- Core·Tor·electrs의 시작·정지·재시작과 정확한 준비 상태 표시.
- 필수 mempool 설정의 적용·잘못된 설정 거부·설정 실패 복구.
- Core 22부터 최신 안정판까지 요구한 선택 범위와 검증된 electrs 조합. 지원 목록을 묵시적으로 줄여 통과시키지 않음.
- 호환되지 않는 버전 전환에서 데이터 보존과 해당 버전으로 시작하는 별도 경로.
- LAN·Tor의 실제 목표 지갑 연결과 QR 입력. 디지털 QR decoder 시험과 실제 휴대폰 카메라 시험을 구분.
- 재부팅·디스크 부족·데이터 마운트 실패·서비스 오류·업데이트 실패의 복구.
- 정상 동기화 상태의 대상 Pi에서 24시간 연속 운영하며 관리 구성요소의 비정상 종료·지속적인 메모리 증가·UI 응답 정지를 확인. 시험 조건과 추이를 기록하며 이 시험 하나로 무결함을 보증하지 않음.
- 새 빌드 환경에서 소스와 잠금된 의존성으로 배포물을 생성할 수 있음.

위의 장시간 운영 시험은 메모리 누수와 서비스 안정성 확인을 위한 릴리스 기준이다. 매 수정마다 무조건 다시 24시간을 기다리지 말고, 수정 영향에 따라 재실행 여부를 판단한다. 모든 검증을 통과한 뒤 요구하지 않은 기능이나 끝없는 추가 테스트로 완료를 지연하지 않는다.

### 사용자가 필요한 예외와 최종 보고

사용자만 해결할 수 있는 문제는 이미 시도한 해결법, 막힌 항목, 필요한 최소 조치를 모아서 설명한다. 처음 발견한 장애 하나 때문에 바로 전체 작업을 종료하지 않는다. 가능한 구현·시험·문서·배포 준비를 모두 진행한 뒤 남은 장애를 보고한다. 이 경우 상태는 `BLOCKED`이며 완료로 바꾸지 않는다.

실행 시간·사용량 한도·세션 종료·권한 정책은 프롬프트와 별개다. 여러 실행에 걸친 자동 진행이 필요하면 실제 설치 환경에서 지원되는 비대화형 실행과 재개 방법을 검증하여 작업 실행기를 구성할 수 있다. 특정 프로젝트 세션 ID, 진행 상태, 오류별 재시도 제한, 사용량/비용 제한, 중지 기능을 갖추고 여러 프로세스가 같은 파일을 동시에 고치지 않게 한다. 전역의 가장 최근 세션을 무조건 재개하지 않는다. [Codex 비대화형 실행·재개](https://learn.chatgpt.com/docs/non-interactive-mode), [Codex 권한](https://learn.chatgpt.com/docs/permissions)

최종 보고는 전체 완료 여부, 소스와 설치 이미지 위치, 실제 통과한 환경·버전 범위, 미검증·차단 항목, 설치·복구 방법을 포함한다. 모든 필수 검증을 충족했을 때만 검증된 배포 후보라고 보고한다. 아직 접근하지 못한 실기 검증이 있으면 소프트웨어 산출물을 전달하면서 그 제한을 명확히 밝힌다.
