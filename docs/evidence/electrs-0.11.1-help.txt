Usage: .cache/electrs-0.11.1/target/release/electrs [ARGUMENTS...]

Arguments:
        --conf                        Load configuration from this file.
        --conf-dir                    Load configuration from files in this 
                                      directory.
        --skip-default-conf-files     Skip loading default configuration files.
        --db-dir                      Directory to store index database (default
                                      : ./db/)
        --db-log-dir                  Directory to store index database internal
                                      log (default: same as specified by `db_dir
                                      `)
        --db-parallelism              Max threads to use for DB background 
                                      operations (flushes and compactions)
        --daemon-dir                  Data directory of Bitcoind (default: ~/.
                                      bitcoin/)
        --cookie-file                 JSONRPC authentication cookie file (
                                      default: ~/.bitcoin/.cookie)
        --network                     Select Bitcoin network type ('bitcoin', '
                                      testnet', 'testnet4', 'regtest' or 'signet
                                      ')
        --electrum-rpc-addr           Electrum server JSONRPC 'addr:port' to 
                                      listen on (default: '127.0.0.1:50001' for 
                                      mainnet, '127.0.0.1:60001' for testnet, '
                                      127.0.0.1:60401' for regtest and '
                                      127.0.0.1:60601' for signet)
        --daemon-rpc-addr             Bitcoin daemon JSONRPC 'addr:port' to 
                                      connect (default: 127.0.0.1:8332 for 
                                      mainnet, 127.0.0.1:18332 for testnet, 
                                      127.0.0.1:18443 for regtest and 127.0.0.1:
                                      18554 for signet)
        --daemon-p2p-addr             Bitcoin daemon p2p 'addr:port' to connect 
                                      (default: 127.0.0.1:8333 for mainnet, 
                                      127.0.0.1:18333 for testnet, 127.0.0.1:
                                      18444 for regtest and 127.0.0.1:38333 for 
                                      signet)
        --monitoring-addr             Prometheus monitoring 'addr:port' to 
                                      listen on (default: 127.0.0.1:4224 for 
                                      mainnet, 127.0.0.1:14224 for testnet, 
                                      127.0.0.1:24224 for regtest and 127.0.0.1:
                                      34224 for signet)
        --wait-duration-secs          Duration to wait between bitcoind polling
        --jsonrpc-timeout-secs        Duration to wait until bitcoind JSON-RPC 
                                      timeouts (must be greater than 
                                      wait_duration_secs).
        --index-batch-size            Number of blocks to get in a single p2p 
                                      protocol request from bitcoind
        --index-lookup-limit          Number of transactions to lookup before 
                                      returning an error, to prevent 'too 
                                      popular' addresses from causing the RPC 
                                      server to get stuck (0 - disable the limit
                                      )
        --reindex-last-blocks         Number of last blocks to reindex (used for
                                      testing)
        --server-banner               The banner to be shown in the Electrum 
                                      console
        --log-filters                 Logging filters, overriding `RUST_LOG` 
                                      environment variable (see https://docs.rs/
                                      env_logger/ for details)
        --magic                       network magic for custom network in hex 
                                      format, as found in Bitcoin Core logs
        -v, --verbose                 Increase logging verbosity
        --timestamp                   Prepend log lines with a timestamp
        --no-auto-reindex             Automatically reindex the database if it's
                                      inconsistent or in old format
        --ignore-mempool              Don't sync mempool - queries will show 
                                      only confirmed transactions.
        --disable-electrum-rpc        Disable Electrum RPC server - only sync 
                                      and index blocks.
        --sync-once                   Exit after the initial sync is over (don't
                                      start Electrum server).
        --skip-block-download-wait    Don't wait for block download to finish 
                                      before starting sync.
        --version                     Print out the program version.
